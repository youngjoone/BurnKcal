# Copy to backend/.env. Values use Java properties syntax (no quotes or export).
# .env is read when the server is started from backend/.
BURNKCAL_ANALYSIS_MODE=demo
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite
# After entering a key, change BURNKCAL_ANALYSIS_MODE to gemini and restart.
